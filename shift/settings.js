var FRAMERATE = 15;
var NUM_FPM = FRAMERATE * 60;
var COUNTER_MAX = FRAMERATE * 1000;
var BPM_MAX = 1500;
var VOLUME_MAX = 10;
var HALF_SECOND = FRAMERATE / 2;
// Takes 10s to reach max volume
let DURATION_MAX = 60;
